<Window x:Class="FarmProductsWPF.category_popup.DeleteCategoryPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FarmProductsWPF.category_popup"
        mc:Ignorable="d"
        Title="Delete Category"
        Height="600"
        Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#F8F9FA">
    <Window.Resources>
        <!-- Button styles -->
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" 
            BorderBrush="#E5E7EB" 
            BorderThickness="1" 
            CornerRadius="8" 
            Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Content Area with ScrollViewer -->
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header with close button -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Title -->
                        <TextBlock Grid.Column="0"
                                   Text="Delete Category"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#111827"/>

                        <!-- Close button -->
                        <Button Grid.Column="1"
                                x:Name="btnClose"
                                Content="✕"
                                FontSize="14"
                                Width="24"
                                Height="24"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="btnClose_Click"/>
                    </Grid>

                    <!-- Confirmation Message -->
                    <TextBlock Grid.Row="1"
                               x:Name="txtConfirmation"
                               Text="Are you sure you want to delete &quot;Vegetables&quot;?"
                               FontSize="14"
                               Foreground="#6B7280"
                               Margin="0,4,0,24"/>

                    <!-- Category Information Panel -->
                    <Border Grid.Row="2"
                            BorderBrush="#E5E7EB"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="#F9FAFB"
                            Padding="16"
                            Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Category Label -->
                            <TextBlock Grid.Row="0"
                                     Grid.Column="0"
                                     Text="Category:"
                                     FontSize="14"
                                     FontWeight="Medium"
                                     Foreground="#111827"
                                     Margin="0,0,10,8"/>

                            <!-- Category Value -->
                            <TextBlock Grid.Row="0"
                                     Grid.Column="1"
                                     x:Name="lblCategoryName"
                                     Text="Vegetables"
                                     FontSize="14"
                                     Foreground="#111827"
                                     Margin="0,0,0,8"/>

                            <!-- Description Label -->
                            <TextBlock Grid.Row="1"
                                     Grid.Column="0"
                                     Text="Description:"
                                     FontSize="14"
                                     FontWeight="Medium"
                                     Foreground="#111827"
                                     Margin="0,0,10,8"/>

                            <!-- Description Value -->
                            <TextBlock Grid.Row="1"
                                     Grid.Column="1"
                                     x:Name="lblDescription"
                                     Text="Fresh organic vegetables"
                                     FontSize="14"
                                     Foreground="#111827"
                                     Margin="0,0,0,8"
                                     TextWrapping="Wrap"/>

                            <!-- Products Label -->
                            <TextBlock Grid.Row="2"
                                     Grid.Column="0"
                                     Text="Products:"
                                     FontSize="14"
                                     FontWeight="Medium"
                                     Foreground="#111827"
                                     Margin="0,0,10,0"/>

                            <!-- Products Value -->
                            <TextBlock Grid.Row="2"
                                     Grid.Column="1"
                                     x:Name="lblProductsCount"
                                     Text="2 products"
                                     FontSize="14"
                                     Foreground="#111827"
                                     Margin="0,0,0,0"/>
                        </Grid>
                    </Border>

                    <!-- Warning Panel -->
                    <Border Grid.Row="3"
                            BorderBrush="#FEF08A"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="#FEFCE8"
                            Padding="16"
                            Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Warning Header with Icon -->
                            <StackPanel Grid.Row="0" 
                                      Orientation="Horizontal" 
                                      Margin="0,0,0,8">
                                <TextBlock Text="⚠️" 
                                         FontSize="16"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Warning: Products in this category"
                                         FontSize="14"
                                         FontWeight="SemiBold"
                                         Foreground="#854D0E"
                                         VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Warning Message -->
                            <TextBlock Grid.Row="3" 
                                     Text="You cannot delete this category until products belong to this category are moved to another category or deleted."
                                     FontSize="14"
                                     Foreground="#854D0E"
                                     TextWrapping="Wrap"
                                     Margin="0,8,0,0"/>
                        </Grid>
                    </Border>

                    <!-- Additional Warning -->
                    <Border Grid.Row="4"
                            BorderBrush="#E5E7EB"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="#F9FAFB"
                            Padding="16"
                            Margin="0,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Warning Title -->
                            <TextBlock Grid.Row="0"
                                     Text="This action cannot be undone."
                                     FontSize="14"
                                     FontWeight="Medium"
                                     Foreground="#111827"
                                     Margin="0,0,0,8"/>

                            <!-- Warning Details -->
                            <TextBlock Grid.Row="1"
                                     Text="The category will be permanently removed from the system."
                                     FontSize="14"
                                     Foreground="#6B7280"
                                     TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>

            <!-- Button Row - Separated from ScrollViewer -->
            <Grid Grid.Row="1" Margin="24,0,24,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Spacer -->
                <Rectangle Grid.Column="0" Fill="Transparent"/>

                <!-- Cancel Button -->
                <Button Grid.Column="1"
                        x:Name="btnCancel"
                        Content="Cancel"
                        Style="{StaticResource CancelButtonStyle}"
                        Margin="0,0,12,0"
                        Width="100"
                        Click="btnCancel_Click"/>

                <!-- Delete Category Button -->
                <Button Grid.Column="2"
                        x:Name="btnDeleteCategory"
                        Content="Delete Category"
                        Style="{StaticResource DeleteButtonStyle}"
                        Width="150"
                        Click="btnDeleteCategory_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
