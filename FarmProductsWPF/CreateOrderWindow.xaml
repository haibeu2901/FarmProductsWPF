<Window x:Class="FarmProductsWPF.CreateOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FarmProductsWPF"
        mc:Ignorable="d"
        Title="Farm Products Management System" 
        Height="800" 
        Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">

    <Window.Resources>
        <!-- Add these colors to your Window.Resources -->
        <SolidColorBrush x:Key="LogoutRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="LogoutRedHover" Color="#DC2626"/>
        <SolidColorBrush x:Key="PrimaryGreen" Color="#27AE60"/>
        <SolidColorBrush x:Key="PrimaryGreenLight" Color="#E8F5E9"/>
        <SolidColorBrush x:Key="WarningYellow" Color="#FEF3C7"/>
        <SolidColorBrush x:Key="WarningYellowBorder" Color="#FCD34D"/>
        <SolidColorBrush x:Key="DangerRed" Color="#FEE2E2"/>
        <SolidColorBrush x:Key="DangerRedBorder" Color="#FCA5A5"/>
        <SolidColorBrush x:Key="NeutralGray" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>

        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="40,0,12,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8"
                        Height="{TemplateBinding Height}">
                            <Grid>
                                <TextBlock Text="🔍" 
                                 Margin="12,0,0,0"
                                 VerticalAlignment="Center"
                                 Foreground="#9CA3AF"/>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Button Style -->
        <Style x:Key="AddButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                             VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Quantity Control Button Style -->
        <Style x:Key="QuantityButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Logout Button Style -->
        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource LogoutRed}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource LogoutRedHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Create Order Button Style -->
        <Style x:Key="CreateOrderButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F3F4"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Style.Resources>
                <!-- Define cell style that preserves text color -->
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <!-- Remove focus visual styles -->
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>

        <!-- DataGrid Column Header Style -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#5a6268"/>
        </Style>

        <!-- DataGrid Column Header Style with centered content -->
        <Style x:Key="CenteredColumnHeaderStyle" TargetType="DataGridColumnHeader" 
               BasedOn="{StaticResource DataGridColumnHeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- DataGrid Cell Style with centered content -->
        <Style x:Key="CenteredCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Category Pill Style -->
        <Style x:Key="StockIndicatorStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PrimaryGreenLight}"/>
            <Setter Property="BorderBrush" Value="#81C784"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Category Pill Style -->
        <Style x:Key="CategoryPillStyle" TargetType="Border">
            <Setter Property="Background" Value="#E8F5E9"/>
            <Setter Property="BorderBrush" Value="#81C784"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Status Pill Style -->
        <Style x:Key="StatusPill" TargetType="Border">
            <Setter Property="Background" Value="#E8F5E9"/>
            <Setter Property="BorderBrush" Value="#81C784"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Watermark Style for TextBox -->
        <Style x:Key="WatermarkStyle" TargetType="TextBox" BasedOn="{StaticResource SearchBox}">
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Background">
                        <Setter.Value>
                            <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                <VisualBrush.Visual>
                                    <TextBlock Text="Search products..." 
                                            Foreground="#9CA3AF" 
                                            FontStyle="Italic"
                                            Margin="40,0,0,0"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomerSearchStyle" TargetType="TextBox" BasedOn="{StaticResource SearchBox}">
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Background">
                        <Setter.Value>
                            <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                <VisualBrush.Visual>
                                    <TextBlock Text="Search by phone number..." 
                                            Foreground="#9CA3AF" 
                                            FontStyle="Italic"
                                            Margin="40,0,0,0"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Add this style to Window.Resources section -->
        <Style x:Key="PriceStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#28A745"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#7C3AED" CornerRadius="12" Width="48" Height="48">
                    <TextBlock Text="⚙️" 
                             FontSize="24" 
                             HorizontalAlignment="Center" 
                             VerticalAlignment="Center"/>
                </Border>
                <StackPanel Margin="16,8,16,8">
                    <TextBlock Text="Farm Products Management System"
                             FontSize="20"
                             FontWeight="Bold"
                             Foreground="#111827"
                             />
                </StackPanel>
            </StackPanel>

            <!-- User Info and Logout -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Margin="0,0,16,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤" 
                             FontSize="16" 
                             Margin="0,0,8,0"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUser.FullName}" 
                             FontWeight="Medium"
                             FontSize="14"
                             Foreground="#1F2937" 
                             VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Logout button -->
                <Button x:Name="btnLogout" 
                        Style="{StaticResource LogoutButtonStyle}" 
                        Click="btnLogout_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🛫 Logout" Padding="5"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Navigation -->
        <Border Grid.Row="1" 
                Background="White"
                Margin="24,0,24,16"
                Height="64">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnViewStockWindow" Grid.Column="0"  Content="📦 View Stock" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnViewStockWindow_Click" />
                <Button Grid.Column="1"
                        Content="🛒 Create Order" Style="{StaticResource NavButton}" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Background="#F3F4F6"
                        Foreground="#4F46E5"/>
                <Button x:Name="btnOrderHistoryWindow"  Grid.Column="2" Content="📅 Order History" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnOrderHistoryWindow_Click"/>
                <Button x:Name="btnAccountsWindow" Grid.Column="3" Content="👥 Accounts" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button x:Name="btnCategoriesWindow" Grid.Column="4" Content="🏷️ Categories" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnCategoriesWindow_Click"/>
                <Button x:Name="btnProductsWindow" Grid.Column="5" Content="🌾 Products" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnProductsWindow_Click"/>
                <Button x:Name="btnStockManagementWindow" Grid.Column="6" Content="📋 Stock Management" Style="{StaticResource NavButton}" HorizontalAlignment="Center" VerticalAlignment="Center" Click="btnStockManagementWindow_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="24,0,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Product Selection with DataGrid -->
            <Border Background="White"
                    CornerRadius="12"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Margin="0,0,12,0"
                    Padding="24">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Select Products" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Choose products for the customer order" 
                                   FontSize="14" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,4,0,16"/>
                    </StackPanel>

                    <!-- Product Search Box -->
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="txtProductSearch" 
                                 Grid.Column="0" 
                                 Background="Transparent" 
                                 BorderThickness="0"
                                 Foreground="{StaticResource TextPrimary}"
                                 VerticalAlignment="Center"
                                 Height="40">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="White"
                                                        BorderBrush="#E5E7EB"
                                                        BorderThickness="1"
                                                        CornerRadius="8"
                                                        Height="40">
                                                    <Grid>
                                                        <TextBlock Text="🔍" 
                                                                 Margin="12,0,0,0"
                                                                 VerticalAlignment="Center"
                                                                 Foreground="#9CA3AF"/>
                                                        <TextBox Text="{Binding Path=Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                                                 Background="Transparent"
                                                                 Foreground="{TemplateBinding Foreground}"
                                                                 VerticalContentAlignment="Center"
                                                                 Padding="40,0,12,0"
                                                                 BorderThickness="0"/>
                                                        <TextBlock Text="Search by name, category, or description..." 
                                                                   Foreground="#9CA3AF" 
                                                                   IsHitTestVisible="False"
                                                                   VerticalAlignment="Center"
                                                                   Margin="40,0,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        
                        <!-- Search Button -->
                        <Button x:Name="btnProductSearch" 
                                Grid.Column="1"
                                Content="🔍 Search"
                                Style="{StaticResource NavButton}"
                                Background="#007BFF"
                                Foreground="White"
                                Margin="10,0,0,0"
                                Padding="20,10"
                                VerticalAlignment="Center" 
                                Click="btnProductSearch_Click">
                            <Button.Resources>
                                <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#0056B3"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <!-- Products DataGrid -->
                    <DataGrid Grid.Row="2" 
                              x:Name="dtgProducts" 
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                              IsReadOnly="True" 
                              Loaded="dtgProducts_Loaded"
                              ScrollViewer.CanContentScroll="False"
                              ScrollViewer.PanningMode="VerticalOnly"
                              ScrollViewer.PanningRatio="0.001"
                              ScrollViewer.PanningDeceleration="0.000001"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.ScrollUnit="Pixel">
                        <!-- Replace the current DataGrid columns section in dtgProducts -->
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*" 
                                                HeaderStyle="{StaticResource CenteredColumnHeaderStyle}"
                                                ElementStyle="{StaticResource CenteredCellStyle}"/>

                            <!-- Category column with pill style -->
                            <DataGridTemplateColumn Header="Category" 
                                                    Width="0.8*"
                                                    HeaderStyle="{StaticResource CenteredColumnHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CategoryPillStyle}">
                                            <TextBlock Text="{Binding CategoryName}"
                                                       Foreground="#2E7D32"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       TextAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Price" 
                                                Binding="{Binding PriceDisplay, StringFormat='{}{0:#,##0}₫'}" 
                                                Width="0.7*"
                                                HeaderStyle="{StaticResource CenteredColumnHeaderStyle}"
                                                ElementStyle="{StaticResource PriceStyle}"/>

                            <!-- Stock Status Column with conditional formatting -->
                            <DataGridTemplateColumn Header="Stock Status" Width="0.7*"
                                                    HeaderStyle="{StaticResource CenteredColumnHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource StatusPill}">
                                                    <Style.Triggers>
                                                        <!-- Out of Stock -->
                                                        <DataTrigger Binding="{Binding StockDisplay}" Value="Out of Stock">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                            <Setter Property="BorderBrush" Value="#FCA5A5"/>
                                                        </DataTrigger>
                                                        <!-- Low Stock -->
                                                        <DataTrigger Binding="{Binding StockDisplay}" Value="Low Stock">
                                                            <Setter Property="Background" Value="#FEF3C7"/>
                                                            <Setter Property="BorderBrush" Value="#FCD34D"/>
                                                        </DataTrigger>
                                                        <!-- In Stock (default state) -->
                                                        <DataTrigger Binding="{Binding StockDisplay}" Value="In Stock">
                                                            <Setter Property="Background" Value="#E8F5E9"/>
                                                            <Setter Property="BorderBrush" Value="#81C784"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="12"
                                                       FontWeight="SemiBold"
                                                       TextAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="{Binding CombinedStockDisplay}"/>
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <!-- Out of Stock -->
                                                            <DataTrigger Binding="{Binding StockDisplay}" Value="Out of Stock">
                                                                <Setter Property="Foreground" Value="#DC2626"/>
                                                            </DataTrigger>
                                                            <!-- Low Stock -->
                                                            <DataTrigger Binding="{Binding StockDisplay}" Value="Low Stock">
                                                                <Setter Property="Foreground" Value="#B45309"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Add Button Column -->
                            <DataGridTemplateColumn Header="" Width="0.5*"
                                                    HeaderStyle="{StaticResource CenteredColumnHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="btnAddProductToCart" 
                                                Content="Add" 
                                                Style="{StaticResource AddButton}"
                                                Command="{Binding AddToCartCommand}"
                                                CommandParameter="{Binding}"
                                                Click="btnAddProductToCart_Click">
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="➕" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Add" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Right Panel - Order Cart with DataGrid -->
            <Border Grid.Column="1"
                    Background="White"
                    CornerRadius="12"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Margin="12,0,0,0"
                    Padding="24">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Cart Header -->
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Order Cart" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Review items before checkout" 
                                   FontSize="14" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,4,0,16"/>
                    </StackPanel>

                    <!-- Customer Selection -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,16">
                        <TextBlock Text="Customer Selection" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,8"/>

                        <!-- Customer Search Box with Button -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox x:Name="txtCustomerSearch" 
                                     Grid.Column="0" 
                                     Background="Transparent" 
                                     BorderThickness="0"
                                     Foreground="{StaticResource TextPrimary}"
                                     VerticalAlignment="Center"
                                     Height="40">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="White"
                                                            BorderBrush="#E5E7EB"
                                                            BorderThickness="1"
                                                            CornerRadius="8"
                                                            Height="40">
                                                        <Grid>
                                                            <TextBlock Text="🔍" 
                                                                     Margin="12,0,0,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#9CA3AF"/>
                                                            <TextBox Text="{Binding Path=Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                                                     Background="Transparent"
                                                                     Foreground="{TemplateBinding Foreground}"
                                                                     VerticalContentAlignment="Center"
                                                                     Padding="40,0,12,0"
                                                                     BorderThickness="0"/>
                                                            <TextBlock Text="Search by phone number..." 
                                                                       Foreground="#9CA3AF" 
                                                                       IsHitTestVisible="False"
                                                                       VerticalAlignment="Center"
                                                                       Margin="40,0,0,0">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            
                            <!-- Customer Search Button -->
                            <Button x:Name="btnCustomerSearch" 
                                    Grid.Column="1"
                                    Content="🔍 Search"
                                    Style="{StaticResource NavButton}"
                                    Background="#007BFF"
                                    Foreground="White"
                                    Margin="10,0,0,0"
                                    Padding="20,10"
                                    VerticalAlignment="Center" 
                                    Click="btnCustomerSearch_Click">
                                <Button.Resources>
                                    <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#0056B3"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>

                        <!-- Customer Info Display Area -->
                        <Grid Margin="0,0,0,12">
                            <!-- Customer Information Panel -->
                            <Border x:Name="customerInfoPanel" 
                                    Background="#E8F5E9"
                                    BorderBrush="#A5D6A7"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="16,12"
                                    Visibility="Collapsed">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock x:Name="txtCustomerName" 
                                                   FontWeight="SemiBold" 
                                                   FontSize="16"
                                                   Foreground="#2E7D32"/>
                                        <TextBlock x:Name="txtCustomerPhone" 
                                                   FontSize="14"
                                                   Foreground="#2E7D32"/>
                                    </StackPanel>

                                    <Button Grid.Column="1"
                                            Content="Clear"
                                            Foreground="#DC3545"
                                            Background="White"
                                            BorderBrush="#DC3545"
                                            BorderThickness="1"
                                            Padding="12,6"
                                            FontSize="13"
                                            FontWeight="Medium"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Click="btnClearCustomer_Click">
                                        <Button.Resources>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    CornerRadius="4"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                 VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#FFF3F4"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <!-- Cart Items DataGrid -->
                    <DataGrid Grid.Row="2" 
                              x:Name="dtgOrderCart" 
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                              IsReadOnly="True"
                              Margin="0,0,0,16" 
                              Loaded="dtgOrderCart_Loaded"
                              ScrollViewer.CanContentScroll="False"
                              ScrollViewer.PanningMode="VerticalOnly"
                              ScrollViewer.PanningRatio="0.001"
                              ScrollViewer.PanningDeceleration="0.000001"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.ScrollUnit="Pixel">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding OrderDetailName}" Width="*"
                                                HeaderStyle="{StaticResource CenteredColumnHeaderStyle}"
                                                ElementStyle="{StaticResource CenteredCellStyle}"/>

                            <DataGridTextColumn Header="Price" 
                                                Binding="{Binding OrderDetailPrice, StringFormat='{}{0:#,##0}₫'}" 
                                                Width="0.7*"
                                                HeaderStyle="{StaticResource CenteredColumnHeaderStyle}"
                                                ElementStyle="{StaticResource PriceStyle}"/>

                            <!-- Quantity Control Column -->
                            <DataGridTemplateColumn Header="Quantity" Width="1.2*"
                                                    HeaderStyle="{StaticResource CenteredColumnHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button x:Name="btnDecreaseItem" Style="{StaticResource QuantityButton}" 
                                                    Content="➖" 
                                                    Command="{Binding DecreaseQuantityCommand}"
                                                    CommandParameter="{Binding}" Click="btnDecreaseItem_Click"/>
                                            <TextBlock Text="{Binding OrderDetailQuantity}" 
                                                       Width="40" 
                                                       TextAlignment="Center" 
                                                       VerticalAlignment="Center"/>
                                            <Button x:Name="btnIncreaseItem" Style="{StaticResource QuantityButton}" 
                                                    Content="➕" 
                                                    Command="{Binding IncreaseQuantityCommand}"
                                                    CommandParameter="{Binding}" Click="btnIncreaseItem_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Remove Button Column -->
                            <DataGridTemplateColumn Header="" Width="0.4*"
                                                    HeaderStyle="{StaticResource CenteredColumnHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="btnRemoveItem" Width="32" 
                                               Height="32" 
                                               Background="Transparent" 
                                               BorderThickness="0"
                                               Command="{Binding RemoveCommand}"
                                               CommandParameter="{Binding}" Click="btnRemoveItem_Click">
                                            <TextBlock Text="🗑" FontSize="14"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Order Total and Checkout -->
                    <Grid Grid.Row="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" 
                                      Text="Total:" 
                                      FontSize="18" 
                                      FontWeight="Bold" 
                                      Foreground="{StaticResource TextPrimary}"/>

                            <TextBlock x:Name="txtOrderTotalPrice" Grid.Column="1" 
                                      Text="{Binding TotalPrice, StringFormat='{}{0:#,##0}₫'}" 
                                      FontSize="18" 
                                      FontWeight="Bold" 
                                      Foreground="#28A745"/>
                        </Grid>

                        <Button x:Name="btnCreateOrder" Grid.Row="1" 
                                Style="{StaticResource CreateOrderButton}"
                                HorizontalAlignment="Stretch" Click="btnCreateOrder_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🛒" Margin="0,0,8,0"/>
                                <TextBlock Text="Create Order"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>